apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: order-service
  
  ports:
    - port: {{ .Values.order.port }}
      targetPort: {{ .Values.order.port }}
