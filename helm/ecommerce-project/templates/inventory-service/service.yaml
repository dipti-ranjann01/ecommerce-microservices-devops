apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: inventory-service
  
  ports:
    - port: {{ .Values.inventory.port }}
      targetPort: {{ .Values.inventory.port }}